@{
    var tagsHtml = new List<IHtmlString>();
    foreach(var t in Model.Tags) {
        //if (tagsHtml.Any()) {
        //    tagsHtml.Add(new HtmlString(", "));
        //}
        //var linkText = "<span>" + t.TagName + "</span>";
        //tagsHtml.Add(Html.ActionLink((string)linkText, "Search", "Home", new { area = "Orchard.Tags", tagName = (string)t.TagName }, new { }));
        tagsHtml.Add(new HtmlString("<a href=" + Url.Action("Search", "Home", new { area = "Orchard.Tags", tagName = (string)t.TagName }) + "><span>" + t.TagName + "</span></a>"));
    }
}

@if (tagsHtml.Any()) {
    <p class="tags">
        @foreach(var htmlString in tagsHtml) { @htmlString }
    </p>
}